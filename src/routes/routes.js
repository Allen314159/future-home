import Home from '../pages/Home/home';
import Login from '../pages/Login/login';

const routes = [
    {
        path: '/',
        component: Login,
        protected: false
    }
]

export default routes;